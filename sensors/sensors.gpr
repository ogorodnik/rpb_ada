with "../gpio/gpio.gpr";
with "../shields/shields.gpr";

project Sensors is

   for Source_Dirs use ("./**");
   for Object_Dir use ".obj";
   for Library_Name use "sensors";

   package Builder is
      for Default_Switches ("ada") use ("-g");
   end Builder;

end Sensors;

